import styled from 'styled-components';

export const HeaderStyle = styled.header`
  padding: 20px;

  nav {
  
  }
    nav ul {
      display: flex;
      flex-direction: row;
      gap: 20px;
      font-size: 20px;
      justify-content: center;
    }
      ul li {
    
      }
`
